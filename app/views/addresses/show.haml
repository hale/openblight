.inner.single
  .show-grid
    .map
      #address{"data-x" => @address.point.x, "data-y" => @address.point.y }
        #map{:style => "height: 200px;"}
    .address
      %h1=@address.address_long

    =render "addresses/progress-bar"
    =render "addresses/status-update"
    =render "addresses/property-history"